This is the canonical OpenHands workspace base mounted into the runtime as `/opt/workspace_base`.

- Keep real task repositories here if they must be exposed to OpenHands sandboxes.
- Do not mix vendored dependencies, archived references, or runtime support code into this tree.
