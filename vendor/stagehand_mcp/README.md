This directory is the extracted active Stagehand MCP runtime slice.

- It is the only Stagehand code path used by `compose/docker-compose.yml`.
- It was trimmed out of a much larger copied Stagehand workspace so the active browser/tool lane is obvious.
- The broader upstream snapshot is retained under `archive/vendor_snapshots/stagehand_working/` for reference only.
