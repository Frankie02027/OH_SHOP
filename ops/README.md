Command surface for OH_SHOP.

- `garagectl.py` is the only normal operator/controller entrypoint.
- `manual/` contains exceptional repair or privileged host helpers that do not belong on the main command surface.
