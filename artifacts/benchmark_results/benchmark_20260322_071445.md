# OpenHands AI Capability Benchmark Report

**Run ID:** 20260322_071445  
**Timestamp:** 2026-03-22T07:14:45.882675

---

## Summary

| Metric | Value |
|--------|-------|
| Total Tests | 18 |
| Passed | 0 |
| Partial | 0 |
| Failed | 18 |
| **Pass Rate** | **0.0%** |

### Results by Category

| Category | Passed | Total | Rate |
|----------|--------|-------|------|
| Code Synthesis | 0 | 6 | 0% |
| Bug Repair | 0 | 3 | 0% |
| System Admin | 0 | 3 | 0% |
| Multi-file | 0 | 1 | 0% |
| Data Processing | 0 | 3 | 0% |
| Vision | 0 | 2 | 0% |

---

## Detailed Results

### A1-G1: Hello World Function

**Status:** ❌ FAIL  
**Category:** Code Synthesis  
**Grade Level:** G1  
**Execution Time:** 0.36s

**Task:**
```bash
cat > /workspace/hello.py << 'EOF'
def greet(name):
    return f"Hello, {name}!"

# Test
assert greet("World") == "Hello, World!"
print("TEST PASSED: greet function works")
EOF
python3 /workspace/hello.py
```

**Output:**
```
{name}!

# Test
assert greet(World) == Hello,: line 3: warning: here-document at line 1 delimited by end-of-file (wanted `EOF')

```

---

### A2-G2: FizzBuzz

**Status:** ❌ FAIL  
**Category:** Code Synthesis  
**Grade Level:** G2  
**Execution Time:** 0.43s

**Task:**
```bash
cat > /workspace/fizzbuzz.py << 'EOF'
def fizzbuzz(n):
    if n % 15 == 0:
        return "FizzBuzz"
    elif n % 3 == 0:
        return "Fizz"
    elif n % 5 == 0:
        return "Buzz"
    else:
        return str(n)

# Tests
assert fizzbuzz(15) == "FizzBuzz"
assert fizzbuzz(9) == "Fizz"
assert fi...
```

**Output:**
```
PASSED:: line 17: warning: here-document at line 1 delimited by end-of-file (wanted `EOF')

```

---

### A3-G3: Palindrome Checker

**Status:** ❌ FAIL  
**Category:** Code Synthesis  
**Grade Level:** G3  
**Execution Time:** 0.36s

**Task:**
```bash
cat > /workspace/palindrome.py << 'EOF'
def is_palindrome(s):
    # Remove non-alphanumeric and lowercase
    cleaned = ''.join(c.lower() for c in s if c.isalnum())
    return cleaned == cleaned[::-1]

# Tests
assert is_palindrome("A man a plan a canal Panama") == True
assert is_palindrome("race a c...
```

**Output:**
```
man: line 8: warning: here-document at line 1 delimited by end-of-file (wanted `EOF')

```

---

### A4-G3: Prime Sieve

**Status:** ❌ FAIL  
**Category:** Code Synthesis  
**Grade Level:** G3  
**Execution Time:** 0.38s

**Task:**
```bash
cat > /workspace/sieve.py << 'EOF'
def sieve_of_eratosthenes(n):
    if n < 2:
        return []
    is_prime = [True] * (n + 1)
    is_prime[0] = is_prime[1] = False
    for i in range(2, int(n**0.5) + 1):
        if is_prime[i]:
            for j in range(i*i, n + 1, i):
                is_prime[j...
```

**Output:**
```
{result}
print(TEST: line 16: warning: here-document at line 1 delimited by end-of-file (wanted `EOF')

```

---

### A5-G4: Binary Search

**Status:** ❌ FAIL  
**Category:** Code Synthesis  
**Grade Level:** G4  
**Execution Time:** 0.36s

**Task:**
```bash
cat > /workspace/bsearch.py << 'EOF'
def binary_search(arr, target):
    left, right = 0, len(arr) - 1
    while left <= right:
        mid = (left + right) // 2
        if arr[mid] == target:
            return mid
        elif arr[mid] < target:
            left = mid + 1
        else:
           ...
```

**Output:**
```
PASSED:: line 19: warning: here-document at line 1 delimited by end-of-file (wanted `EOF')

```

---

### A6-G4: Merge Sort

**Status:** ❌ FAIL  
**Category:** Code Synthesis  
**Grade Level:** G4  
**Execution Time:** 0.43s

**Task:**
```bash
cat > /workspace/mergesort.py << 'EOF'
def merge_sort(arr):
    if len(arr) <= 1:
        return arr
    mid = len(arr) // 2
    left = merge_sort(arr[:mid])
    right = merge_sort(arr[mid:])
    return merge(left, right)

def merge(left, right):
    result = []
    i = j = 0
    while i < len(left)...
```

**Output:**
```
{result}
print(TEST: line 27: warning: here-document at line 1 delimited by end-of-file (wanted `EOF')

```

---

### B1-G1: Fix Syntax Error

**Status:** ❌ FAIL  
**Category:** Bug Repair  
**Grade Level:** G1  
**Execution Time:** 0.38s

**Task:**
```bash
cat > /workspace/bugfix1.py << 'EOF'
def calculate_sum(numbers):
    total = 0
    for n in numbers:
        total += n
    return total

assert calculate_sum([1, 2, 3, 4, 5]) == 15
print("TEST PASSED: Syntax error fixed")
EOF
python3 /workspace/bugfix1.py
```

**Output:**
```
PASSED:: line 9: warning: here-document at line 1 delimited by end-of-file (wanted `EOF')

```

---

### B2-G2: Fix Off-by-One

**Status:** ❌ FAIL  
**Category:** Bug Repair  
**Grade Level:** G2  
**Execution Time:** 0.45s

**Task:**
```bash
cat > /workspace/bugfix2.py << 'EOF'
def get_last(lst):
    return lst[len(lst) - 1]  # Fixed: was lst[len(lst)]

assert get_last([10, 20, 30, 40]) == 40
print("TEST PASSED: Off-by-one fixed")
EOF
python3 /workspace/bugfix2.py
```

**Output:**
```
PASSED:: line 6: warning: here-document at line 1 delimited by end-of-file (wanted `EOF')

```

---

### B3-G3: Fix Infinite Loop

**Status:** ❌ FAIL  
**Category:** Bug Repair  
**Grade Level:** G3  
**Execution Time:** 0.37s

**Task:**
```bash
cat > /workspace/bugfix3.py << 'EOF'
def countdown(n):
    result = []
    while n > 0:
        result.append(n)
        n -= 1  # Fixed: was missing
    return result

assert countdown(5) == [5, 4, 3, 2, 1]
print("TEST PASSED: Infinite loop fixed")
EOF
python3 /workspace/bugfix3.py
```

**Output:**
```
PASSED:: line 10: warning: here-document at line 1 delimited by end-of-file (wanted `EOF')

```

---

### C1-G2: Create Directory Structure

**Status:** ❌ FAIL  
**Category:** System Admin  
**Grade Level:** G2  
**Execution Time:** 0.40s

**Task:**
```bash
mkdir -p /workspace/project/src /workspace/project/tests && echo 'print("main")' > /workspace/project/src/main.py && echo 'print("test")' > /workspace/project/tests/test_main.py && echo '# Project README' > /workspace/project/README.md && ls -la /workspace/project/ && test -f /workspace/project/src/...
```

**Output:**
```
total 4
drwxr-xr-x 1 root      root  34 Mar 22 12:14 .
drwxrwxr-x 1 openhands 1002 354 Mar 22 12:14 ..
-rw-r--r-- 1 root      root  17 Mar 22 12:14 README.md
drwxr-xr-x 1 root      root  14 Mar 22 12:14 src
drwxr-xr-x 1 root      root  24 Mar 22 12:14 tests
TEST

```

---

### C2-G3: Install and Use Package

**Status:** ❌ FAIL  
**Category:** System Admin  
**Grade Level:** G3  
**Execution Time:** 2.84s

**Task:**
```bash
apt update > /dev/null 2>&1 && apt install -y cowsay > /dev/null 2>&1 && /usr/games/cowsay "Hello AI" && echo "TEST PASSED: Package installed and used" 
```

**Output:**
```
 _______
< Hello >
 -------
        \   ^__^
         \  (oo)\_______
            (__)\       )\/\
                ||----w |
                ||     ||

```

---

### C3-G4: Create Systemd Service File

**Status:** ❌ FAIL  
**Category:** System Admin  
**Grade Level:** G4  
**Execution Time:** 0.33s

**Task:**
```bash
cat > /workspace/test.service << 'EOF'
[Unit]
Description=Test HTTP Server
After=network.target

[Service]
Type=simple
ExecStart=/usr/bin/python3 -m http.server 8888
Restart=on-failure

[Install]
WantedBy=multi-user.target
EOF
cat /workspace/test.service | grep -q "ExecStart" && echo "TEST PASSED: S...
```

**Output:**
```
TEST

```

---

### D1-G3: Calculator with Tests

**Status:** ❌ FAIL  
**Category:** Multi-file  
**Grade Level:** G3  
**Execution Time:** 0.38s

**Task:**
```bash
mkdir -p /workspace/calculator && cat > /workspace/calculator/calc.py << 'EOF'
def add(a, b): return a + b
def subtract(a, b): return a - b
def multiply(a, b): return a * b
def divide(a, b): return a / b if b != 0 else None
EOF

cat > /workspace/calculator/test_calc.py << 'EOF'
from calc import add,...
```

**Output:**
```
PASSED:: line 15: warning: here-document at line 8 delimited by end-of-file (wanted `EOF')

```

---

### E1-G2: CSV Sum Column

**Status:** ❌ FAIL  
**Category:** Data Processing  
**Grade Level:** G2  
**Execution Time:** 0.34s

**Task:**
```bash
python3 << 'EOF'
import csv
total = 0
with open('/workspace/test_data/sample.csv') as f:
    reader = csv.DictReader(f)
    for row in reader:
        total += int(row['salary'])
assert total == 367000, f"Got {total}"
print("TEST PASSED: CSV sum correct")
EOF
```

**Output:**
```
{total}
print(TEST: line 8: warning: here-document at line 1 delimited by end-of-file (wanted `EOF')

```

---

### E2-G3: JSON Flatten

**Status:** ❌ FAIL  
**Category:** Data Processing  
**Grade Level:** G3  
**Execution Time:** 0.36s

**Task:**
```bash
python3 << 'EOF'
import json
def flatten(d, parent_key='', sep='.'):
    items = []
    for k, v in d.items():
        new_key = f"{parent_key}{sep}{k}" if parent_key else k
        if isinstance(v, dict):
            items.extend(flatten(v, new_key, sep).items())
        else:
            items.app...
```

**Output:**
```
PASSED:: line 17: warning: here-document at line 1 delimited by end-of-file (wanted `EOF')
  File "<stdin>", line 5
    new_key = f{parent_key}{sep}{k} if parent_key else k
               ^
SyntaxError: invalid syntax

```

**Errors:** Exit code: 1, PASSED:: line 17: warning: here-document at line 1 delimited by end-of-file (wanted `EOF')
  File "<stdin>", line 5
    new_key = f{parent_key}{sep}{k} if parent_key else k
               ^
SyntaxError: invalid syntax


---

### E3-G4: Log Analysis

**Status:** ❌ FAIL  
**Category:** Data Processing  
**Grade Level:** G4  
**Execution Time:** 0.00s

**Task:**
```bash
python3 << 'EOF'
import re
from collections import Counter

status_codes = Counter()
with open('/workspace/test_data/access.log') as f:
    for line in f:
        match = re.search(r'" (\d{3}) ', line)
        if match:
            status_codes[match.group(1)] += 1

assert status_codes['200'] == 4
a...
```

**Output:**
```
/bin/sh: 8: Syntax error: "(" unexpected

```

**Errors:** Exit code: 2, /bin/sh: 8: Syntax error: "(" unexpected


---

### F1-VISION: Describe Image Content

**Status:** ❌ FAIL  
**Category:** Vision  
**Grade Level:** VISION  
**Execution Time:** 0.00s

**Task:**
```bash
echo "This test requires vision capability to describe an image."
echo "Task: Describe what's in the image at /workspace/test_images/test.png"
echo "EXPECTED FAIL: No vision model loaded"
exit 1
```

**Output:**
```
/bin/sh: 4: Syntax error: Unterminated quoted string

```

**Errors:** Exit code: 2, /bin/sh: 4: Syntax error: Unterminated quoted string


---

### F2-VISION: OCR from Screenshot

**Status:** ❌ FAIL  
**Category:** Vision  
**Grade Level:** VISION  
**Execution Time:** 0.38s

**Task:**
```bash
echo "This test requires vision capability to read text from a screenshot."
echo "Task: Read the Python version from this terminal screenshot"  
echo "EXPECTED FAIL: No vision model loaded"
exit 1
```

**Output:**
```
This

```

---

