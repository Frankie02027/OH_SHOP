Compatibility debt lives here.

- `openhands_sdk_overrides/` contains the shared OpenHands converter overlays still required by the current runtime.
- These files are active, but they are not clean product architecture.
- Keep new compatibility shims here instead of burying them inside otherwise-normal runtime folders.
